# CircleCI configuration file
version: 2.1

executors:
  my-executor:
    docker:
      - image: cimg/base:2022.05

jobs:
  # Build
  build:
    executor: my-executor
    steps:
      - checkout
      - run: pwd
      - run: ls
      - run: cmake -Bbuild ./
      - run: (cd ./build && make)
      - run: pwd
      - run: ls
      - run: ls build
      - run: build/test
      - persist_to_workspace:
          root: build
          paths:
            - test

  # Test
  test:
    executor: my-executor
    steps:
      - run: pwd
      - run: ls
      - attach_workspace:
          at: build
      - run: pwd
      - run: ls
      - run: ls build

workflows:
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
